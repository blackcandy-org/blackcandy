<% if params[:update_action] == 'delete' && @playlist.empty? %>
  <% partial = (params[:id].in? %w(current favorite) ? 'playlist/show' : 'song_collections/show' %>
  App.renderPlaylistContent('<%= j(render partial) %>');
<% end %>

<%= render_flash %>
