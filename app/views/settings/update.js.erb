App.renderContent('#js-setting-form', '<%= j(render 'form') %>');
<%= render_flash %>

if ('<%= Current.user.theme %>' != document.body.dataset.themeName) {
  document.body.dataset.themeName = '<%= Current.user.theme %>'
  App.dispatchEvent('body', 'theme:update');
}
